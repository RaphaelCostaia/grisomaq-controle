{
  "name": "grisomaq-servidor",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "API do GRISOMAQ CONTROLE: autenticação por PIN e sincronização offline",
  "engines": {
    "node": ">=22.6"
  },
  "scripts": {
    "dev": "node --watch src/index.ts",
    "start": "node src/index.ts",
    "migrar": "node src/migracoes.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --test src/**/*.teste.ts"
  },
  "dependencies": {
    "@fastify/cors": "^11.0.1",
    "@fastify/rate-limit": "^10.2.2",
    "fastify": "^5.6.0",
    "jose": "^6.1.0",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.6",
    "@types/node": "^24.13.3",
    "@types/pg": "^8.15.5",
    "typescript": "^5.9.3"
  }
}
